Это довольно интересная программа — она работает со **строками** и выполняет **замену первого и последнего слова** в каждой введённой строке.

Разберём её подробно и просто 👇

---

## 🧩 Краткое описание

Программа:

1. Просит пользователя ввести несколько строк (до 10).
2. Для каждой строки **меняет местами первое и последнее слово**.
3. Выводит исходный и изменённый вариант.

---

## 🔍 Подробный разбор

### 🔸 Объявление типов и переменных

```pascal
type mas = array[1..10] of string;
var F: mas; i, n: integer; T: string;
```

* `mas` — массив из 10 строк.
* `F` — хранит введённые пользователем строки.
* `n` — количество строк, которое пользователь введёт.
* `T` — временная строка для результата после замены.

---

### 🔸 Функция `zamena`

Это «сердце» программы — именно она делает перестановку слов.

```pascal
Function zamena(F: string): string;
var
  fpozit, D, Q: integer;
  sper, sposobled: string;
```

Переменные:

* `fpozit` — позиция **первого пробела**;
* `D` — длина строки;
* `Q` — позиция **последнего пробела** (ищется с конца);
* `sper`, `sposled` — первое и последнее слово.

---

### 🔸 Логика замены

Пошагово:

```pascal
fpozit := Pos(' ', F);
sper := Copy(F, 1, fpozit);
```

👉 Находим первый пробел, копируем **первое слово** в `sper`.

---

```pascal
D := Length(F);
Q := D;
while F[Q] <> ' ' do Q := Q - 1;
sposled := Copy(F, Q + 1, D - Q + 1);
```

👉 Ищем **последний пробел**,
копируем **последнее слово** в `sposled`.

---

Теперь удаляются эти слова из исходной строки:

```pascal
Delete(F, Q, D - Q + 1);
Delete(F, 1, fpozit);
```

👉 Из строки убрали первое и последнее слово.

---

Далее добавляем их обратно, поменяв местами:

```pascal
Insert(' ', F, 1);
Insert(sposled, F, 1);
D := Length(F);
Insert(' ', sper, 1);
Insert(sper, F, D + 1);
```

В итоге:

* В начало вставляем **последнее слово**,
* В конец — **первое слово**,
* Между ними остаётся «середина» исходной строки.

---

### 🔸 Основная программа

```pascal
writeln('Введите количество строк'); readln(n);
for i := 1 to n do
begin
  writeln('Вводите строку'); readln(F[i]);
end;
```

👉 Пользователь вводит строки (например, предложения).

---

Затем обработка и вывод:

```pascal
for i := 1 to n do
begin
  writeln(' ');
  writeln('Вывожу исходный текст');
  writeln(F[i]);
  T := zamena(F[i]);
  writeln(' ');
  write(' ':20);
  writeln('Вывожу конечный текст');
  write(' ':20);
  writeln(T);
  writeln;
end;
```

👉 Для каждой строки:

* выводится исходный вариант,
* вызывается функция `zamena`,
* затем показывается преобразованный текст.

---

## 🧮 Пример работы

**Ввод:**

```
Введите количество строк
1
Вводите строку
Я люблю программировать на Паскале
```

**Результат:**

```
Вывожу исходный текст
Я люблю программировать на Паскале

                    Вывожу конечный текст
                    Паскале люблю программировать на Я
```

---

## ⚠️ Ограничения и замечания

1. Программа предполагает, что **в строке есть хотя бы два слова** (иначе будет ошибка при поиске пробела).
2. Если в строке больше двух слов — всё между ними остаётся на месте.
3. Не обрабатываются ситуации с двойными пробелами.

---

## ✅ Итог

**Назначение программы:**

> Программа меняет местами **первое и последнее слово** в каждой введённой строке и выводит результат.
