# 🗺️ Пояснительная записка к программе «Region»

## 1. Назначение программы

Программа **«Region»** предназначена для хранения, редактирования и просмотра информации о странах и их формах государственного устройства.
Она моделирует простейшую систему учёта государств, позволяя пользователю вводить данные о стране, континенте и политической системе — президентской, парламентской или смешанной.

Данные сохраняются в бинарный файл `LAND.DAT`, который служит внутренней базой данных программы.
Программа работает в интерактивном режиме с удобным текстовым меню.

---

## 2. Структура данных

Основной элемент хранения — запись `Reg`, содержащая сведения о стране и её политической системе.
В записи используется **вариантная часть** (`case of`), которая позволяет хранить разные поля в зависимости от формы правления.

```pascal
type
  Gos_Us = (Prez, Parl, Smes);   // формы государственного устройства

  Reg = record
    n: integer;                  // номер записи
    Country: String[30];         // название страны
    Land: String[30];            // континент
    case ustr: Gos_Us of
      Prez: (FIO_Prez: String[50]; Sr_Doh: Real);   // президентская
      Parl: (FIO_Prem: String[50]; Nas: LongInt);   // парламентская
      Smes: (Kol: Integer; Chis: Integer);          // смешанная
  end;
```

**Тип `Gos_Us`** задаёт три возможные формы государственного устройства:

* `Prez` — президентская республика;
* `Parl` — парламентская республика;
* `Smes` — смешанная форма правления.

---

## 3. Принцип работы программы

После запуска программа отображает главное меню:

```
==========================================
          СИСТЕМА УЧЁТА СТРАН
==========================================
1 - Ввод и редактирование записей
2 - Просмотр записей
0 - Выход
------------------------------------------
```

### Режим 1. Ввод и редактирование записей

Пользователь поочерёдно вводит:

1. Название страны;
2. Название континента;
3. Тип государственного устройства — выбирается цифрой (1, 2 или 3).

В зависимости от выбранного типа программа запрашивает дополнительные данные:

| Тип устройства | Дополнительные поля                  |
| -------------- | ------------------------------------ |
| Президентская  | ФИО президента, средний доход        |
| Парламентская  | ФИО премьера, численность населения  |
| Смешанная      | Количество партий, число спидоносных |

После ввода всех данных запись сохраняется в файл `LAND.DAT`.
Программа проверяет корректность числовых значений — если введено не число, пользователь получает предупреждение и повторный ввод.

Пользователь может:

* добавить новую запись (клавиша Enter);
* завершить ввод (клавиша Esc).

---

### Режим 2. Просмотр записей

Программа открывает файл `LAND.DAT` и построчно выводит всю сохранённую информацию:

```
------------------------------------------
СПИСОК СТРАН:
------------------------------------------
Страна: Франция
Континент: Европа
Тип: Президентская
Президент: Эммануэль Макрон
Средний доход: 2500.00
------------------------------------------
```

Для каждой записи выводятся только поля, соответствующие типу государственного устройства.
После просмотра можно вернуться в меню.

---

## 4. Структура программы

Программа состоит из следующих основных блоков:

1. **Объявление типов и переменных.**
2. **Главное меню выбора режима.**
3. **Блок редактирования (ввод данных и запись в файл).**
4. **Блок просмотра (чтение и вывод данных из файла).**
5. **Заключение и выход.**

Основные операции с файлом:

* `Rewrite(land)` — создание нового файла;
* `Write(land, x)` — запись новой записи;
* `Reset(land)` — открытие файла для чтения;
* `Read(land, x)` — чтение записи;
* `Close(land)` — закрытие файла.

---

## 5. Особенности реализации

* Программа использует **модуль CRT** для очистки экрана (`ClrScr`) и обработки клавиш (`ReadKey`).
* Встроенные проверки защищают от ошибок ввода чисел.
* Реализована **вариантная структура данных**, что экономит память и повышает читаемость.
* Интерфейс дружелюбен и понятен, все сообщения на русском языке.
* Файл `LAND.DAT` создаётся в том же каталоге, где запущена программа.

---

## 6. Пример работы программы

```
Выберите действие:
1 - Ввод и редактирование записей
2 - Просмотр записей
0 - Выход
Ваш выбор: 1

Введите название страны: Германия
Введите название континента: Европа
Введите тип государственного устройства:
1 - президентская
2 - парламентская
3 - смешанная
Ваш выбор: 2
Введите Ф.И.О. премьера: Олаф Шольц
Введите население: 83000000
Запись успешно добавлена!
```

---

## 7. Заключение

Программа **«Region»** является учебным примером работы с:

* вариантными записями (`case of`),
* файлами типа `file of record`,
* пользовательским вводом и обработкой ошибок.

Она демонстрирует принципы структурного программирования на Pascal, позволяет закрепить навыки работы с данными и обеспечивает наглядную реализацию простой базы данных.

---