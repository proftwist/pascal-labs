# 🗺️ Пояснительная записка к программе «Region»

## 1. Назначение программы

Программа **«Region»** предназначена для хранения и обработки информации о странах и их формах государственного устройства.
Она позволяет пользователю вводить данные о странах, включая название, континент и тип политической системы
(**президентская**, **парламентская** или **смешанная**), а также сохранять эти сведения в файл.

Программа может работать в двух режимах:

* **редактирование** — ввод новых записей о странах;
* **просмотр** — чтение уже существующих данных.

---

## 2. Структура данных

Основная структура данных описана с помощью **записи с вариантной частью**, которая меняется в зависимости от формы правления:

```pascal
type
  Gos_Us = (Prez, Parl, Smes);
  Reg = record
    n: integer;              // номер записи
    Country: String[20];     // название страны
    Land: String[20];        // континент
    case ustr: Gos_Us of
      Prez: (FIO_Prez: String[50]; Sr_Doh: Real);
      Parl: (FIO_Prem: String[50]; Nas: Integer);
      Smes: (Kol: Integer; Chis: Integer);
  end;
```

Тип `Gos_Us` задаёт возможные формы государственного устройства:

* `Prez` — президентская республика;
* `Parl` — парламентская республика;
* `Smes` — смешанная форма правления.

Таким образом, запись `Reg` может принимать разные структуры в зависимости от выбранного значения `ustr`.

---

## 3. Принцип работы программы

После запуска программа очищает экран и предлагает пользователю выбрать одно из двух действий:

1. **Редактирование записей** – ввод новых данных о странах.
2. **Просмотр записей** – чтение существующего файла и вывод информации.

### В режиме редактирования

Пользователь поочерёдно вводит:

1. Название страны;
2. Континент;
3. Тип государственного устройства.

После выбора формы правления программа запрашивает дополнительные сведения:

| Тип устройства | Дополнительные поля                    |
| -------------- | -------------------------------------- |
| Президентская  | ФИО президента, средний доход          |
| Парламентская  | ФИО премьера, численность населения    |
| Смешанная      | Количество партий, число «спидоносных» |

После заполнения всех полей данные записываются в файл `LAND.DAT`, который является **базой данных программы**.

---

## 4. Структура программы

Программа состоит из одного основного блока, в котором реализованы:

* объявление типов и переменных;
* меню выбора действий;
* блок редактирования (ввод и запись данных в файл);
* блок чтения (просмотр данных).

Основные операции:

1. `Rewrite(land)` — создаёт новый файл или очищает существующий;
2. `Write(land, x)` — записывает введённую запись;
3. `Reset(land)` — открывает файл для чтения;
4. `Close(land)` — закрывает файл перед завершением работы.

---

## 5. Особенности реализации

* Используется **вариантная запись** (`case of`), что позволяет экономно хранить данные разных типов в одной структуре.
* Данные записываются в **бинарный файл** (`file of record`).
* Применяются функции из модуля **CRT** для очистки экрана (`ClrScr`) и считывания клавиш (`ReadKey`).
* Программа легко расширяется: можно добавить сортировку, удаление или поиск по заданным параметрам.

---

## 6. Пример работы программы

```
Выберите действие:
   1: редактирование записей
   2: просмотр записей

Для ввода данных нажмите Enter, для окончания работы - *

Введите название страны: Франция
Введите название континента: Европа
Введите тип государственного устройства: президентская
Введите Ф.И.О. президента: Эммануэль Макрон
Введите средний доход: 2500

Данные успешно записаны в файл LAND.DAT.
```

---

## 7. Заключение

Программа **«Region»** демонстрирует основы:

* работы с **файлами** и **записями** в Паскале;
* использования **вариантных структур** (`case of`);
* построения простого **пользовательского интерфейса** на основе текстовых меню.

Она служит учебным примером по курсу «Основы программирования», показывая, как организовать хранение и обработку данных в структурированном виде.