# 🩺 Пояснительная записка к программе «Krank»

## 1. Назначение программы

Программа **«Krank»** предназначена для работы с данными о пациентах медицинского учреждения.
Она позволяет:

* хранить информацию о пациентах (фамилия, пол, возраст, город, диагноз);
* выводить весь список пациентов в виде таблицы;
* выполнять **поиск пациентов** по заданному диагнозу и минимальному возрасту.

Таким образом, программа моделирует простую информационную систему для медицинского регистратора или врача-аналитика.

---

## 2. Структура программы

Программа построена модульно, что облегчает её поддержку и расширение.
Проект состоит из следующих модулей:

| Модуль                            | Назначение                                                                     |
| --------------------------------- | ------------------------------------------------------------------------------ |
| **Typ.pas**                       | Хранит глобальные типы данных и переменные.                                    |
| **Writ.pas**                      | Реализует обработку запросов (поиск пациентов по диагнозу и возрасту).         |
| **Prov.pas**                      | Проверяет корректность введённых пользователем данных (в частности, возраста). |
| **Spis.pas**                      | Отвечает за вывод всего списка пациентов в табличной форме.                    |
| **Rea.pas** *(предположительно)*  | Считывает данные из файла и заполняет массив записей `dan`.                    |
| **Sort.pas** *(предположительно)* | Реализует сортировку массива пациентов (возможно, по фамилии или возрасту).    |
| **Main.pas**                      | Главная программа, обеспечивающая пользовательский интерфейс.                  |

---

## 3. Структура данных

В модуле `Typ.pas` определена основная структура:

```pascal
type
  Patient = record
    fam: string[20];   // фамилия
    pol: char;         // пол (М/Ж)
    voz: integer;      // возраст
    gorod: string[20]; // город
    diag: string[20];  // диагноз
  end;
```

Массив `dan: array[1..100] of Patient` содержит список пациентов.
Переменная `bul: file of Patient` используется для хранения и загрузки данных из файла.

---

## 4. Описание модулей

### 🧩 Модуль **Typ.pas**

Содержит все общие типы и переменные, используемые другими модулями.
Это — “ядро данных” программы.

### 🧩 Модуль **Writ.pas**

Реализует процедуру:

```pascal
procedure obrab(d, v: string);
```

Здесь `d` — диагноз, `v` — минимальный возраст.
Процедура проходит по массиву `dan` и выводит пациентов, у которых:

* диагноз совпадает с введённым пользователем;
* возраст больше или равен указанному.

Если подходящие пациенты не найдены, программа сообщает об этом пользователю.

### 🧩 Модуль **Prov.pas**

Реализует процедуру:

```pascal
procedure proverka;
```

Проверяет правильность введённых данных:

* убеждается, что возраст — это число;
* возраст не меньше 1 и не больше 110;
* при успешной проверке вызывает `obrab(diagn, vozr)` из модуля `Writ`.

Это — своеобразный «фильтр ввода» перед поиском.

### 🧩 Модуль **Spis.pas**

Реализует процедуру:

```pascal
procedure obr;
```

Форматно выводит весь список пациентов в виде таблицы с рамками, используя символы `chr(201)`, `chr(205)`, `chr(186)` и т. д.
В таблице показываются колонки:

| Фамилия | Пол | Возраст | Город | Диагноз |

Длина каждого столбца выравнивается вручную с помощью пробелов.

### 🧩 Модуль **Main.pas**

Главный модуль программы.

Основная логика:

1. Очищает экран (`ClrScr`).
2. Загружает данные пациентов (`Re` — процедура чтения из файла).
3. Выводит меню:

   * `1` — показать весь список (`obr` из `Spis`),
   * `2` — ввести запрос (вызов `proverka` из `Prov`).
4. После выполнения операции предлагает выйти (`1 - да, 2 - нет`).

---

## 5. Принцип работы программы

1. При запуске программа очищает экран и загружает данные из файла.
2. Пользователю предлагается выбрать действие:

   * Просмотр всех пациентов.
   * Поиск по диагнозу и возрасту.
3. Если выбран просмотр — выводится красивая таблица всех записей.
4. Если выбран поиск — программа запрашивает диагноз и минимальный возраст, проверяет корректность данных и выводит результаты.
5. После каждого действия можно выйти из программы или продолжить.

---

## 6. Особенности реализации

* Используется **модульная структура**, что повышает читаемость и масштабируемость.
* Применяется **валидация данных** (проверка возраста на корректность).
* Форматированный вывод с использованием псевдографики (`chr(201)` и др.).
* Возможность дальнейшего расширения (например, сортировки или добавления новых пациентов).

---

## 7. Пример сценария работы

```
Введите действие (1 - просмотреть весь список, 2 - ввести запрос): 2

Введите диагноз: Грипп
Введите минимальный возраст: 25

Результаты поиска:
==================
Фамилия: Иванов, Пол: М, Возраст: 34, Город: Москва
Фамилия: Соколова, Пол: Ж, Возраст: 42, Город: Пермь

Хотите выйти из программы? (1 - да, 2 - нет): 1
```

---

## 8. Вывод

Программа **«Krank»** — это учебная система обработки данных пациентов, написанная на языке Pascal.
Она демонстрирует:

* работу с **записями и массивами**;
* использование **файлового ввода/вывода**;
* применение **модульного подхода** в программировании;
* **валидацию пользовательского ввода** и **форматированный вывод** данных.