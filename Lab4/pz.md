Программа написана на Паскале и работает с **двумя массивами вещественных чисел**.

---

## 🧩 Краткое описание

Программа:

1. Запрашивает у пользователя **два массива чисел**.
2. Для первого массива считает, **сколько положительных элементов**.
3. Для второго массива — **сколько отрицательных элементов**.
4. Выводит оба массива и количество найденных элементов (или сообщение, что таких нет).

---

## 🔍 Подробный разбор

### 🔸 Объявление переменных

```pascal
type mas = array[1..100] of real;
var
  x, y: mas;
  m, n: integer;
  kolich: integer;
  i: integer;
  L: boolean;
```

* `mas` — тип массива из 100 вещественных чисел.
* `x`, `y` — сами массивы.
* `n`, `m` — фактическое количество элементов в каждом массиве.
* `kolich` — количество найденных положительных/отрицательных элементов.
* `L` — логическая переменная (true / false), чтобы управлять режимом поиска.
* `i` — индекс цикла.

---

### 🔸 Процедура `input`

```pascal
Procedure input(var k: integer; var F: mas);
Begin
  writeln('Введите количество элементов'); readln(k);
  for i := 1 to k do
  begin
    writeln('Введите элемент массива'); readln(F[i]);
  end;
End;
```

👉 Считывает массив:

* сначала спрашивает, сколько элементов;
* затем по очереди вводит значения и записывает их в массив `F`.

---

### 🔸 Процедура `vivod`

```pascal
Procedure vivod(k: integer; F: mas);
Begin
  for i := 1 to k do
    write(F[i]:10:2);
End;
```

👉 Просто красиво выводит массив на экран с двумя знаками после запятой.

---

### 🔸 Функция `lich`

```pascal
Function lich(k: integer; F: mas; L: boolean): integer;
var kol: integer;
Begin
  kol := 0;
  for i := 1 to k do
  begin
    if L = true then
    begin
      if F[i] > 0 then kol := kol + 1;
    end
    else
      if F[i] < 0 then kol := kol + 1;
  end;
  lich := kol;
End;
```

👉 Считает количество элементов в массиве по условию:

* если `L = true` → считает **положительные** (`> 0`),
* если `L = false` → считает **отрицательные** (`< 0`).

---

### 🔸 Основная программа

```pascal
input(n, x);
input(m, y);

L := true;
kolich := lich(n, x, L);
writeln('Вывожу элементы первого массива');
vivod(n, x);
writeln(' Кол-во полож.', kolich:3);
if kolich = 0 then writeln('Пол. элем отсутствуют');

L := false;
kolich := lich(m, y, L);
writeln('Вывожу элементы второго массива');
vivod(m, y);
writeln(' Кол-во отр.', kolich:3);
if kolich = 0 then writeln('Отр. элем отсутствуют');
```

🔹 Вводятся два массива: `x` и `y`.
🔹 Для первого (`x`) считает положительные элементы.
🔹 Для второго (`y`) — отрицательные.
🔹 Каждый раз выводит массив и результаты подсчёта.

---

## 🧮 Пример работы

```
Введите количество элементов
3
Введите элемент массива
-1
Введите элемент массива
2
Введите элемент массива
3
Введите количество элементов
4
Введите элемент массива
-5
Введите элемент массива
0
Введите элемент массива
7
Введите элемент массива
-2
```

Результат:

```
Вывожу элементы первого массива
    -1.00     2.00     3.00 Кол-во полож.  2
Вывожу элементы второго массива
    -5.00     0.00     7.00    -2.00 Кол-во отр.  2
```

---

## ✅ Итог

**Назначение программы:**

> Программа обрабатывает два массива вещественных чисел:
>
> * Для первого находит количество положительных элементов;
> * Для второго — количество отрицательных;
>   и выводит результаты на экран.