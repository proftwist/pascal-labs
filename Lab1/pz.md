### 💡 Назначение

Программа **вводит число `a`** и **вычисляет `r = 2 * a`**,
после чего **выводит результат**.

---

### 🔍 Разбор кода

```pascal
var a, r: real;
```

Переменные `a` и `r` — вещественные (могут хранить дробные числа).

---

```pascal
write('a='); 
readln(a);
```

Просит пользователя ввести значение `a`.
Например:

```
a=3.5
```

---

```pascal
r := 2 * a;
```

Вычисляет удвоенное значение `a` и сохраняет его в `r`.

---

```pascal
writeln('r=', r:6:1);
```

Выводит результат `r` с форматированием:

* ширина поля — 6 символов,
* 1 знак после запятой.

Например, если `a = 3.5`, то:

```
r=   7.0
```

---

### 🧩 Итого

| Шаг | Действие              |
| --- | --------------------- |
| 1   | Вводит число `a`      |
| 2   | Вычисляет `r = 2 × a` |
| 3   | Выводит `r` на экран  |

---

### 🧮 Пример

```
a=4.2
r=   8.4
```

---

### ✅ Кратко

> Программа просто **удваивает введённое число**.
> Если ввести `a = 5`, получим `r = 10`.
