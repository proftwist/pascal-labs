## 🧩 Общая идея

Программа под названием `opros` (опрос) — это **мини-тест**, который просит пользователя ввести число.
Она анализирует это число по определённым правилам, и сообщает, **подходит ли оно** (выполняет ли заданные условия).

У пользователя есть **три попытки** — после трёх ошибок программа завершает работу.

---

## 🔍 Пошаговый разбор

### 🔸 Объявление переменных

```pascal
var 
  k : integer;
  z : string;
  f : boolean;
```

* `k` — счётчик попыток;
* `z` — введённая пользователем строка (возможно число);
* `f` — логический флаг, показывает, подходит ли введённое значение.

---

### 🔸 Процедура `analiz(a, f)`

Эта процедура проверяет строку `a` и устанавливает `f := true`, если значение удовлетворяет условиям.

#### Внутри:

```pascal
q := Length(a);
f := false;
```

Сначала узнаёт длину строки.

---

#### Если длина < 5

```pascal
if q < 5 then begin
  val(a, con, os);
  if (os = 0) and (con mod 2 = 0) then f := true
end
```

🔹 `val` пытается преобразовать строку `a` в число `con`.
Если `os = 0` — значит преобразование успешно.
Если число **чётное**, то `f := true`.

👉 **То есть:** если введено число с 1–4 цифрами, и оно чётное, — оно подходит.

---

#### Если длина = 5

```pascal
if q = 5 then begin
  f := true;
  t := copy(a,1,4);
  val(t, con, os);
  if os = 0 then begin
    if con <= 3276 then begin
      t := Copy(a,5,1);
      val(t, con, os);
      if os = 0 then begin
        if con <= 8 then begin
          b := con mod(2);
          if b = 0 then f := true
        end
        else f := false
      end
      else f := false
    end
    else f := false
  end
  else f := false
end;
```

🔹 Проверяется более сложный случай — пятизначное число:

1. Берутся первые 4 цифры → проверяется, ≤ 3276
2. Берётся последняя цифра → проверяется, ≤ 8
3. И последняя цифра должна быть **чётной**

Если всё это верно → `f := true`, иначе — `false`.

👉 **То есть:** если введено пятизначное число ≤ 32768 и оно чётное, оно тоже подходит.

---

#### Все остальные случаи → `f := false`

(число длиннее 5 знаков, не число, или нечётное).

---

### 🔸 Процедура `Help`

```pascal
Procedure Help;
Begin
  TextColor(2);
  k := k + 1;
  writeln('Подсказка: нужно ввести число, которое делится на 2');
End;
```

Если пользователь введёт `?`, программа покажет **подсказку**, но при этом **потеряет одну попытку** (увеличивает `k`).

---

### 🔸 Основная программа

```pascal
ClrScr;
TextColor(15);
writeln('Эта программа проверяет ваши знания чисел.');
k := 1;
while k < 4 do begin
  writeln('Введите любое целое число в диапазоне от -32767 до 32768');
  writeln('Если не знаете — наберите "?" для подсказки');
  readln(z);
```

🔹 У пользователя максимум **3 попытки** (`k < 4`).
🔹 Если вводит `"?"` — вызывается `Help`.
🔹 Иначе вызывается `analiz(z, f)`.

---

#### Проверка результата

```pascal
if f = true then begin
  TextColor(9);
  writeln('Вы абсолютно правы! Поздравляю!');
  k := 5;
end
else begin
  TextColor(12);
  writeln('Неверный ответ.');
  k := k + 1;
  if k = 4 then writeln('Вы исчерпали все попытки.')
  else writeln('Попробуйте ещё раз.');
end;
```

* Если число подошло (`f = true`) → сообщение об успехе и выход из цикла.
* Если нет → сообщение об ошибке, счётчик увеличивается.
* После 3 ошибок программа завершает работу.

---

## 🧮 Пример диалога

```
Эта программа проверяет ваши знания чисел.
Введите любое целое число в диапазоне от -32767 до 32768
Если не знаете — наберите "?" для подсказки

Введите число: 123
Неверный ответ.
Попробуйте ещё раз.

Введите число: ?
Подсказка: нужно ввести число, которое делится на 2

Введите число: 32678
Вы абсолютно правы! Поздравляю!
```

---

## ✅ Итого

**Назначение программы:**

> Программа — учебная игра-опрос.
> Она предлагает пользователю ввести число (до 5 знаков),
> проверяет, является ли оно **чётным** и не превышает **32768**,
> и сообщает, правильно ли выполнено задание.
> Пользователю даётся **3 попытки**, есть возможность получить подсказку (`?`).