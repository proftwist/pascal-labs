### 💡 Что делает программа

Это программа на Паскале, которая:

1. **Просит пользователя ввести число** `x`;
2. **Вычисляет факториал** этого числа:
   [
   f = 1 \times 2 \times 3 \times \dots \times x
   ]
3. **Выводит результат** на экран.

---

### 🔍 Пошагово

```pascal
var x, f: integer;
    z: integer;
```

Все переменные — целые (`integer`).
В отличие от `SmallInt`, здесь диапазон шире (обычно от −2 147 483 648 до 2 147 483 647),
поэтому можно вычислить факториалы **до 12!** без переполнения
(так как 13! = 6 227 020 800 — уже больше 2 147 483 647).

---

```pascal
f := 1;
for z := 1 to x do
  f := f * z;
```

Классический цикл для факториала.
Если `x = 5`, то:

```
f = 1×2×3×4×5 = 120
```

---

```pascal
writeln(f);
```

Выводит результат.

---

### ✅ Пример работы

```
Введите число, для которого нужно посчитать факториал
6
720
```

---

### ⚠️ Замечание

Если ввести слишком большое число (например, 15),
результат может быть неверным из-за **переполнения integer**.

Если нужно считать **факториалы больших чисел**, стоит:

* заменить тип `integer` на `Int64`, или
* использовать строки/массивы для хранения длинных чисел (или библиотеку «big integers»).

---

### 🧩 Итого

🟩 **Назначение:** программа вычисляет факториал введённого числа `x`.
🟨 **Формула:**
[
x! = 1 \times 2 \times 3 \times \dots \times x
]
🟥 **Ограничение:** работает корректно примерно до `x = 12`.
